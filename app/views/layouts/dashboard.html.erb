<% content_for :content do %>

  <%= render "shared/nav" %>

  <div class="grid grid-cols-1 md:grid-cols-3 xl:grid-cols-4 gap-5 md:gap-10 lg:gap-20">
    <div class="md:col-span-1">
      sidebar
      <%= link_to 'Link', content_for?(:title) ? yield(:title) : "" %>

    </div>
    <div class="md:col-span-2 xl:col-span-3 space-y-5">
      <%= yield %>
    </div>
  </div>
<% end %>

<%= render template: "layouts/application" %>